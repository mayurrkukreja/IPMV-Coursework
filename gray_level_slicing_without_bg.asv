clc
clear all
%f=[1 2 3; 4 5 6; 7 2 1]
f=imread('cameraman.tif');
r1=70
r2=200
[row,col]=size(f);
for x=1:1:row
    for y=1:1:col
        if f(x,y)>=r1 && f(x,y)<=r2
            g(x,y)=255;
        else
            g(x,y)=0;
        end
    end
end
figure(1),imshow(f)
figure(2),imshow(g)
%g